import { Stack } from "expo-router";

export default function HomeStackLayout() {
  return (
    <Stack screenOptions={{ headerShown: false }}>
      {/* index.tsx = HomeMain */}
      {/* all-products.tsx = AllProducts */}
    </Stack>
  );
}