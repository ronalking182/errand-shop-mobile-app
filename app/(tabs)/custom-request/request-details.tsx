import React from 'react';
import RequestDetailsScreen from '../../../src/screens/CustomRequest/RequestDetailsScreen';

export default function RequestDetailsIndex() {
  return <RequestDetailsScreen navigation={undefined} route={undefined} />;
}